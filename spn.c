#include "spn.h"

void configure(spn_build_ctx_t* b) {
  // spn_target_t* spn = spn_get_target(b, "spn");
  // const spn_build_ctx_t* tcc = spn_get_dep(b, "spcc");
  // const c8* dir = spn_get_subdir(tcc, SPN_DIR_LIB, "tcc");
  // spn_target_embed_dir_ex(spn, dir, "u8", "u64");
  // spn_target_embed_file_ex(spn, "include/spn.h", "include_spn_h", "u8", "u64");
}
