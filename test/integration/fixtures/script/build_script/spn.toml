[package]
name = "build_script"
version = "0.0.1"

[package.configure]
source = ["tools/configure.c"]

[package.build]
source = ["tools/build.c"]
include = ["include"]

[deps.build]
probe = { path = "vendor/probe" }

[[bin]]
name = "build_script"
source = ["main.c"]
