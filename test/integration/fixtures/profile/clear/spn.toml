[package]
name = "profile_clear"
version = "0.1.0"

[profile.default]
sanitize = ["address"]

[profile.clean]
sanitize = []

[[bin]]
name = "main"
source = ["main.c"]
define = [
  { value = "WANT_ASAN", when = { sanitize_address = true } },
]
