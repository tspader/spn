[package]
name = "profile_gated"
version = "0.1.0"

[[bin]]
name = "main"
source = ["main.c"]

[profile.default]
abi = { value = "gnu", when = { os = "linux" } }
opt = { value = "3", when = { os = "macos" } }
