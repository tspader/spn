[package]
name = "options_defaults_false"
version = "0.1.0"

[deps.package]
codec = { version = "1.0.0", options = { ogg = true } }

[config.codec]
defaults = false

[[bin]]
name = "main"
source = ["main.c"]
