[package]
name = "libb"
version = "1.0.0"

[deps.package]
codec = { version = "1.0.0", options = { audio = false, video = true } }

[[lib]]
name = "libb"
kinds = ["static"]
source = ["b.c"]
headers = ["b.h"]
