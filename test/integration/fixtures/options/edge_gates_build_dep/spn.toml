[package]
name = "options_edge_gates_build_dep"
version = "0.1.0"

[deps.package]
tool = { version = "file://./vendor/tool/spn.toml", options = { codegen = true } }

[[bin]]
name = "main"
source = ["main.c"]
