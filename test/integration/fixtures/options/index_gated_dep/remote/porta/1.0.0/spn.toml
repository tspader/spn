[package]
name = "porta"
version = "1.0.0"

[options.jack]
type = "bool"
default = false

[deps.package]
jacklib = { version = "1.0.0", when = { jack = true } }

[[lib]]
name = "porta"
kinds = ["static"]
source = ["porta.c"]
headers = ["porta.h"]
