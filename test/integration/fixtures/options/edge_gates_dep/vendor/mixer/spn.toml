[package]
name = "mixer"
version = "1.0.0"

[options.flac]
type = "bool"
default = false
define = "MIXER_FLAC"

[deps.package]
flaclib = { version = "1.0.0", when = { flac = true } }

[[lib]]
name = "mixer"
kinds = ["static"]
source = ["mixer.c"]
headers = ["mixer.h"]
