[package]
name = "test"
version = "0.1.0"

[deps.package]
spum = { path = "packages/spum" }

[[toolchain]]
name = "custom"
compiler = "cc"
archiver = "ar"
driver = "gcc"
target = [{ @HOST@ }]
cxx = "c++"

[profile.default]
toolchain = "custom"
linkage = "shared"

[[bin]]
name = "main"
source = ["main.c"]
