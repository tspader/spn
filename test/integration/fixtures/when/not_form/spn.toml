[package]
name = "when_not_form"
version = "0.1.0"

[[bin]]
name = "main"
source = ["main.c"]
define = [
  { value = "NOT_MSVC", when = { abi = { not = "msvc" } } },
  { value = "NOT_DEBUG", when = { mode = { not = "debug" } } },
]
