[package]
name = "when_dep_edge"
version = "0.1.0"

[deps.package]
plat = { version = "1.0.0", when = { os = { not = "wasi" } } }
wsock = { version = "1.0.0", when = { os = "wasi" } }

[[bin]]
name = "main"
source = ["main.c"]
