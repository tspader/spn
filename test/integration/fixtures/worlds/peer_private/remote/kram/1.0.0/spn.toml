[package]
name = "kram"
version = "1.0.0"

[deps.package]
spum = { version = "^1.0.0", peer = true, private = true }

[[lib]]
name = "kram"
kinds = ["static"]
source = ["kram.c"]
headers = ["kram.h"]
