# One Linux container per toolchain family. Each runs the whole smoke binary;
# cases self-gate on the programs they need, so a lane runs only its cells and
# skips the rest. Image dirs resolve under test/smoke/variants first, then
# test/tools/docker/variants.
#
# variant             toolchain
debian-zig            zig
debian-llvm           llvm
debian-mingw-clang    clang
debian-cross          gcc
