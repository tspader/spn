[package]
name = "p"
version = "1.0.0"

[profile.release]
toolchain = "zig"
linkage = "shared"
standard = "c99"
mode = "release"
opt = "3"
sanitize = ["address", "undefined"]
os = "linux"
arch = "x86_64"
abi = "gnu"

[profile.clean]
sanitize = []
