[package]
name = "p"
version = "1.0.0"

[profile.default]
abi = [
  { value = "gnu", when = { os = "linux" } },
  { value = "msvc", when = { os = "windows", arch = { not = "aarch64" } } },
  "musl",
]
toolchain = [{ value = "clang", when = { os = "macos" } }]
linkage = { value = "static", when = { os = "linux" } }
mode = ["debug"]
