fuzz repro: mode={{.mode}} iter={{.iter}} (replay: SPN_FUZZ_ITER={{.iter}}, seed printed above)
  run_fixture(utest_result, (fixture_t) {
    .index = {
{{for .pkgs}}
      {
        .namespace = "spn",
        .name = "{{.name}}",
        .releases = {
{{for .releases}}
          {
            .version = spn_semver_lit({{.version}}),
{{for .with_deps}}
            .deps = {
{{for .deps}}
              { .namespace = "spn", .name = "{{.name}}", .version = "{{.range}}"{{.attrs}} },
{{end}}
            },
{{end}}
{{for .with_targets}}
            .targets = {
              { .name = "{{.target}}", .linkages = { SPN_LIB_KIND_SHARED } },
            },
{{end}}
          },
{{end}}
        }
      },
{{end}}
    },
    .manifest = {
      .deps.package = {
{{for .roots}}
        { .name = "spn/{{.name}}", .version = "{{.range}}"{{.attrs}} },
{{end}}
      }
    },
    .err = {{.err}},
  });
