fuzz repro: mode={{.mode}} iter={{.iter}} (replay: --iter {{.iter}}, seed printed above)
  {
    .name = "repro_{{.iter}}",
    .linkage = {{.linkage}},
{{for .with_budget}}
    .budget = {{.budget}},
{{end}}
    .err = {{.err}},
  },

--- fixtures/<suite>/repro_{{.iter}}/spn.toml
[package]
namespace = "test"
name = "root"
version = "0.0.1"
{{for .root_kinds}}

[deps.{{.table}}]
{{for .deps}}
'spn/{{.name}}' = { version = "{{.range}}"{{.attrs}} }
{{end}}
{{end}}
{{for .config}}

[config.'{{.name}}']
kind = "{{.kind}}"
{{end}}
{{for .pkgs}}

--- fixtures/<suite>/repro_{{.iter}}/spn/{{.name}}.jsonl
{{for .releases}}
{{.line}}
{{end}}
{{end}}
