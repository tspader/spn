diff --git a/sp.h b/sp.h
index fc646c5..928e166 100644
--- a/sp.h
+++ b/sp.h
@@ -12337,7 +12337,14 @@ sp_ps_output_t sp_ps_output(sp_ps_t* ps) {
       read_any |= sp_ps_win32_read_available(ps->io.err.fd, &err.base, &err_open) > 0;
     }
 
-    if (!process_done && ps->os->pid) {
+    if (process_done) {
+      if (!read_any) {
+        break;
+      }
+      continue;
+    }
+
+    if (ps->os->pid) {
       DWORD wait = WaitForSingleObject(ps->os->pid, read_any ? 0 : 10);
       if (wait == WAIT_OBJECT_0) {
         process_done = true;
@@ -12349,10 +12356,6 @@ sp_ps_output_t sp_ps_output(sp_ps_t* ps) {
         exit_code = (DWORD)-1;
       }
     }
-
-    if (!read_any && process_done && !out_open && !err_open) {
-      break;
-    }
   }
 
   if (ps->os->pid) {
