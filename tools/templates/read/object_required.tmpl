  {
    toml_table_t* child = toml_table_table(table, "{{ .key }}");
    if (child) {
      spn_codegen_push_key(ctx, "{{ .key }}");
      spn_cg_{{ .object }}_read(ctx, child, &out->{{ .key }});
      spn_codegen_pop(ctx);
    } else {
      spn_codegen_issue(ctx, SPN_CODEGEN_ERR_MISSING_KEY, "{{ .key }}");
    }
  }
