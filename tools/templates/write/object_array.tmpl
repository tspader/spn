    sp_da_push(*out, '[');
    sp_da_for(in->{{ .key }}, it) {
      if (it) {
        sp_da_push(*out, ',');
      }
      spn_cg_{{ .object }}_write(&in->{{ .key }}[it], out);
    }
    sp_da_push(*out, ']');
